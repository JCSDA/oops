obs operator:
  obs type: Stream
obs space:
  source: truth
  obsdatain:  
    obsfile: $(experiment_dir)/{{current_cycle}}/obs.stream.{{current_cycle}}.nc
  obsdataout: 
    obsfile: $(experiment_dir)/{{current_cycle}}/obs.stream.$(experiment).{{current_cycle}}.nc
  obs type: Stream
obs error:
  covariance model: diagonal
filename: obs space.obsdatain.obsfile
