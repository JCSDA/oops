obs operator:
  obs type: WSpeed
obs space:
  source: truth
  obsdatain: 
    obsfile: $(current_dir)/obs.wspeed.{{window_begin}}.nc
  obsdataout:
    obsfile: $(current_dir)/obs.wspeed.$(experiment).{{window_begin}}.nc
  obs type: WSpeed
obs error:
  covariance model: diagonal
r2d2_type: qg_wspeed
