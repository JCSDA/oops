window begin: '{{window_begin}}'
window length: $(window_length)
forecast length: $(window_length)
geometry:
  $(GEOMETRY)
initial condition:
  $(BACKGROUND)
model:
  $(MODEL)
observations:
  $(OBSERVATIONS)
