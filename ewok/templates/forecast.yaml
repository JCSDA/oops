forecast length: $(forecast_length)
initial condition:
  $(INITIAL_CONDITION)
geometry:
  $(GEOMETRY)
model:
  $(MODEL)
output:
  type: fc
  exp: $(expid)
  date: '{{current_cycle}}'
  frequency: $(forecast_output_frequency)
