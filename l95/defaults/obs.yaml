obs operator: {}
obs space:
  source: truth
  filetype: obt
  obsdatain:  '$(expdir)/{{current_cycle}}/input.obs.{{current_cycle}}.obt'
  obsdataout: '$(expdir)/{{current_cycle}}/$(experiment).obs.{{current_cycle}}.obt'
obs error:
  covariance model: diagonal
