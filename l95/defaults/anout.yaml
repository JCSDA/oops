datadir: '$(experiment_dir)/{{current_cycle}}'
filename: '$(experiment_dir)/{{current_cycle}}/$(experiment).an.{{current_cycle}}'
exp: $(experiment)
##first: '{% $(window_length) - $(window_offset) %}'
first: PT3H
frequency: PT06H
type: an
